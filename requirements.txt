# Finger Mouse — runtime and build dependencies, pinned to what CI builds with.
# Python 3.12 (CI) or newer. Install with:  python -m pip install -r requirements.txt
# then fetch the hand model once:           python tools/fetch_model.py

# Hand tracking. MediaPipe 1.0 has no Intel-Mac build, so Intel Macs use
# 0.10.14 (what Finger Mouse 2.1 shipped with there), with the last JAX that
# still builds for Intel Macs. Both versions use the same Tasks API.
mediapipe==1.0.1; sys_platform != "darwin" or platform_machine != "x86_64"
opencv-contrib-python==5.0.0.93; sys_platform != "darwin" or platform_machine != "x86_64"
mediapipe==0.10.14; sys_platform == "darwin" and platform_machine == "x86_64"
opencv-contrib-python==4.11.0.86; sys_platform == "darwin" and platform_machine == "x86_64"
jax==0.4.38; sys_platform == "darwin" and platform_machine == "x86_64"
jaxlib==0.4.38; sys_platform == "darwin" and platform_machine == "x86_64"
numpy<2; sys_platform == "darwin" and platform_machine == "x86_64"

PySide6-Essentials==6.11.2
PyAutoGUI==0.9.54                       # Linux (X11) pointer backend; fallback elsewhere

# macOS: pointer events, permission checks, camera names
pyobjc-framework-Quartz>=11.0; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=11.0; sys_platform == "darwin"
pyobjc-framework-AVFoundation>=11.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=11.0; sys_platform == "darwin"

# Building and testing
pyinstaller==6.22.3
pytest>=8
pillow>=11                              # tools/make_icons.py only
