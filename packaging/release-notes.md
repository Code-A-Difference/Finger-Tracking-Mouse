## Download

| System | File |
|---|---|
| Windows 10/11, 64-bit | `FingerMouse-windows-x64-setup.exe` — installer, no administrator rights needed |
| macOS 13+, Apple silicon | `FingerMouse-macos-arm64.dmg` |
| macOS 13+, Intel | `FingerMouse-macos-x64.dmg` |
| Linux x86-64 (glibc 2.35+, X11) | `FingerMouse-linux-x86_64.AppImage` — `chmod +x`, then run |

`SHA256SUMS.txt` lists a checksum for every file. Or use the download page:
https://codeadifference.ct.ws/projects/finger-tracking-mouse/

New apps can meet a Windows SmartScreen or macOS Gatekeeper warning even when
signed, until the operating system's reputation checks know them. See the
README for what to expect on each system.
